# Generated by Dock-u-Pilot 🐳
# An intelligent Docker configuration generator
# Repository: https://github.com/your-org/dock-u-pilot
# Generated on: 2025-06-19 16:29:24 UTC
# 
# This configuration has been optimized for security, performance, and best practices
# Review and customize as needed for your specific requirements

version: '3.8'
services:
  app:
    build:
      context: .
      target: production
    container_name: personal-web-prod
    ports:
    - 8080:8080
    environment:
    - NODE_ENV=production
    - APP_ENV=production
    restart: unless-stopped
    security_opt:
    - no-new-privileges:true
    read_only: true
    tmpfs:
    - /tmp:rw,noatime,nosuid,nodev,noexec,relatime,size=100m
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M
          cpus: '0.5'
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8080/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
networks:
  default:
    driver: bridge
